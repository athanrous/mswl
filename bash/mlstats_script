 #!/bin/bash

mls=`cat mailing_lists` #Open the mailing_lists text file
echo $mls # Print all the ml's
for ml in $mls
  do
  
  mlstats --­­db­user root --­­db­password root --­­db­name test_ml --­­db-­admin-­user root --­­db-­admin-­password root ml
  
  done
 
